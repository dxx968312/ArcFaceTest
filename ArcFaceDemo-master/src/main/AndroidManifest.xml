<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arcsoft.sdk_demo">
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- camera -->
    <uses-permission android:name="android.permission.CAMERA1" />
    <!-- sdcard -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE1" />
    <!-- wifi internet -->
    <uses-permission android:name="android.permission.INTERNET1" />

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:icon="@mipmap/img4"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PermissionAcitivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".DetecterActivity"></activity>
        <activity android:name=".Main1Activity"></activity>
        <activity android:name=".loginActivity"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".SlitherActivity"></activity>
        <activity android:name=".LogoActivity"></activity>
        <activity android:name=".Logo_Activity"></activity>
        <activity android:name=".SlideActivity"></activity>
        <activity android:name=".ListViewStringActivity"></activity>
        <activity android:name=".ListViewPersonActivity"></activity>
        <activity android:name=".GridViewActivity"></activity>


    </application>

</manifest>